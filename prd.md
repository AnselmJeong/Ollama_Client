# Ollama Desktop Client with Web Search
## Product Requirements Document (PRD)

**버전**: 1.0  
**작성일**: 2026년 2월 2일  
**프로젝트명**: Ollama Desktop Pro  

---

## 1. 개요

### 1.1 제품 비전
로컬 LLM의 강력함과 인터넷 검색의 최신 정보를 결합한 개인용 AI 데스크톱 클라이언트. 기존 Ollama Desktop App의 모든 기능을 재현하면서 웹 검색 에이전트 기능을 추가하여 더욱 풍부하고 정확한 대화 경험을 제공한다.

### 1.2 목표 사용자
- 개인 연구자 및 학술 종사자
- 개발자 및 기술 전문가
- 로컬 LLM을 활용하고자 하는 프라이버시 중시 사용자
- 최신 정보와 함께 AI 대화를 원하는 일반 사용자

### 1.3 핵심 가치 제안
1. **프라이버시 보장**: 로컬 LLM 기반으로 데이터가 외부로 유출되지 않음
2. **최신 정보 접근**: 필요시 웹 검색을 통해 실시간 정보 획득
3. **다양한 파일 지원**: 문서, 코드, 이미지 등 다양한 파일 형식 업로드 및 분석
4. **생산성 향상**: 대화 내용 복사 및 마크다운 내보내기 기능

---

## 2. 기존 Ollama Desktop App 분석

### 2.1 UI 구조 (스크린샷 기반)

#### 메인 레이아웃
```
┌─────────────────────────────────────────────────────────┐
│  🔴 🟡 🟢                    [ ]                         │
├─────────────────┬───────────────────────────────────────┤
│                 │                                       │
│  📝 New Chat    │                                       │
│                 │                                       │
│  ───────────    │            🐕 (마스코트)              │
│  This week      │                                       │
│  · 대화 1...    │                                       │
│  · 대화 2...    │     ┌─────────────────────────┐       │
│                 │     │  Send a message         │       │
│  ───────────    │     │                    + ▾  │       │
│  Older          │     │         model-name:tag  │       │
│  · 대화 3...    │     └─────────────────────────┘       │
│                 │                                       │
└─────────────────┴───────────────────────────────────────┘
```

#### 대화 화면
- 사용자 메시지: 우측 정렬, 배경 박스
- AI 응답: 좌측 정렬, 마크다운 렌더링
- "Thought for X seconds" 표시 (thinking 모드)
- 복사 버튼 (응답 하단)

#### 파일 업로드
- 드래그 앤 드롭 지원
- "Drop files here or paste from clipboard" 메시지
- + 버튼 클릭으로 파일 선택 다이얼로그

#### 설정 화면
- 계정 정보 (이름, 이메일)
- Expose Ollama to the network 토글
- Model location 경로 설정
- Context length 슬라이더 (4k ~ 256k)

### 2.2 기존 기능 목록
1. 새 채팅 생성
2. 채팅 히스토리 (시간별 그룹화)
3. 모델 선택 드롭다운
4. 메시지 전송
5. 스트리밍 응답
6. Thinking 모드 지원
7. 파일 업로드 (드래그 앤 드롭, 버튼 클릭)
8. 응답 복사
9. 설정 관리

---

## 3. 기능 요구사항

### 3.1 핵심 기능 (기존 재현)

#### 3.1.1 채팅 관리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| C-01 | 새 채팅 생성 | 새로운 대화 세션 시작 | P0 |
| C-02 | 채팅 히스토리 | 이전 대화 목록 표시 (시간별 그룹화) | P0 |
| C-03 | 채팅 삭제 | 특정 대화 세션 삭제 | P1 |
| C-04 | 채팅 검색 | 대화 내용 검색 | P2 |

#### 3.1.2 메시지 처리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| M-01 | 메시지 전송 | 텍스트 메시지 전송 | P0 |
| M-02 | 스트리밍 응답 | 실시간 토큰 단위 응답 표시 | P0 |
| M-03 | Thinking 표시 | 모델의 사고 과정 표시 | P0 |
| M-04 | 마크다운 렌더링 | 응답의 마크다운 형식 렌더링 | P0 |
| M-05 | 코드 하이라이팅 | 코드 블록 구문 강조 | P0 |
| M-06 | 메시지 복사 | 개별 메시지 클립보드 복사 | P0 |

#### 3.1.3 모델 관리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| MD-01 | 모델 선택 | 사용 가능한 모델 목록에서 선택 | P0 |
| MD-02 | 모델 정보 표시 | 선택된 모델명 및 태그 표시 | P0 |
| MD-03 | 모델 새로고침 | 사용 가능한 모델 목록 갱신 | P1 |

#### 3.1.4 설정
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| S-01 | Ollama 연결 설정 | 로컬/원격 Ollama 서버 연결 | P0 |
| S-02 | 모델 저장 경로 | 모델 파일 위치 설정 | P1 |
| S-03 | Context Length | 컨텍스트 길이 조절 (4k~256k) | P0 |
| S-04 | 네트워크 노출 | Ollama 네트워크 공유 설정 | P2 |

### 3.2 신규 기능 (Web Search)

#### 3.2.1 웹 검색 에이전트
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| W-01 | 검색 토글 | 메시지별 웹 검색 활성화/비활성화 | P0 |
| W-02 | 자동 검색어 생성 | LLM이 사용자 질문 기반 검색어 자동 생성 | P0 |
| W-03 | 웹 검색 실행 | Ollama webSearch API 호출 | P0 |
| W-04 | 페이지 Fetch | 검색 결과 URL 내용 가져오기 | P0 |
| W-05 | 검색 결과 표시 | 검색 소스 및 참조 링크 표시 | P0 |
| W-06 | 검색 상태 표시 | 검색 중 진행 상태 표시 | P1 |

#### 3.2.2 에이전트 동작 흐름
```
사용자 질문 + [🔍 Search 활성화]
        │
        ▼
┌───────────────────────────────┐
│ LLM: 검색 필요성 판단         │
│ → 필요시 검색어 생성 (tool call)│
└───────────────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│ webSearch(query, max_results) │
│ → 검색 결과 반환              │
└───────────────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│ LLM: 추가 정보 필요 판단      │
│ → 필요시 webFetch(url) 호출   │
└───────────────────────────────┘
        │
        ▼
┌───────────────────────────────┐
│ LLM: 최종 응답 생성           │
│ + 소스 인용                   │
└───────────────────────────────┘
```

### 3.3 파일 업로드 기능

#### 3.3.1 지원 파일 형식
| 카테고리 | 확장자 | 처리 방식 |
|----------|--------|-----------|
| 문서 | .pdf, .md, .txt | 텍스트 추출 후 컨텍스트 포함 |
| 코드 | .js, .ts, .py, .java, .cpp, .c, .go, .rs, .rb, .php, .swift, .kt 등 | 구문 강조 + 분석 |
| 이미지 | .png, .jpg, .jpeg, .gif, .webp | Base64 인코딩 (vision 모델용) |
| 데이터 | .json, .yaml, .xml, .csv | 구조 파싱 후 분석 |

#### 3.3.2 업로드 인터페이스
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| F-01 | 드래그 앤 드롭 | 파일을 채팅 영역으로 드래그 | P0 |
| F-02 | 버튼 클릭 업로드 | + 버튼으로 파일 선택 다이얼로그 | P0 |
| F-03 | 클립보드 붙여넣기 | Cmd+V로 이미지 붙여넣기 | P1 |
| F-04 | 다중 파일 | 여러 파일 동시 업로드 | P1 |
| F-05 | 파일 미리보기 | 업로드된 파일 썸네일/정보 표시 | P0 |
| F-06 | 파일 제거 | 업로드 취소 | P0 |

### 3.4 프로젝트 기능

#### 3.4.1 프로젝트 개념
프로젝트는 관련된 대화들을 그룹화하고, 프로젝트 내 과거 대화 내용을 새 대화의 컨텍스트로 자동 참조할 수 있게 하는 기능이다.

```
┌─────────────────────────────────────────────────────────────┐
│                        Projects                              │
├─────────────────────────────────────────────────────────────┤
│  📁 "논문 작성 프로젝트"                                      │
│     ├── 💬 서론 작성 도움 (1/15)                             │
│     ├── 💬 방법론 검토 (1/20)                                │
│     ├── 💬 결과 분석 (1/25)                                  │
│     └── 💬 [새 대화] ← 위 3개 대화의 맥락을 자동 참조         │
│                                                              │
│  📁 "웹 개발 프로젝트"                                        │
│     ├── 💬 React 구조 설계                                   │
│     └── 💬 API 설계 논의                                     │
└─────────────────────────────────────────────────────────────┘
```

#### 3.4.2 프로젝트 관리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| P-01 | 프로젝트 생성 | 새 프로젝트 생성 (이름, 설명, 아이콘) | P0 |
| P-02 | 프로젝트 목록 | 사이드바에 프로젝트 목록 표시 | P0 |
| P-03 | 프로젝트 편집 | 이름, 설명 수정 | P1 |
| P-04 | 프로젝트 삭제 | 프로젝트 및 포함된 대화 삭제 | P1 |
| P-05 | 프로젝트 아카이브 | 사용하지 않는 프로젝트 숨김 | P2 |

#### 3.4.3 프로젝트 내 대화 관리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| P-06 | 프로젝트 내 새 대화 | 특정 프로젝트 내에서 새 대화 시작 | P0 |
| P-07 | 대화 이동 | 기존 대화를 다른 프로젝트로 이동 | P1 |
| P-08 | 대화 복사 | 대화를 다른 프로젝트에 복사 | P2 |

#### 3.4.4 컨텍스트 참조 시스템 (핵심 기능)

##### 동작 원리
```
새 대화 시작 시:
┌─────────────────────────────────────────────────────────────┐
│ 1. 프로젝트 내 과거 대화들에서 요약/핵심 내용 추출            │
│    └── 각 대화의 주요 결론, 결정사항, 핵심 정보              │
│                                                              │
│ 2. 시스템 프롬프트에 컨텍스트로 주입                         │
│    └── "이 프로젝트의 이전 대화 맥락: ..."                   │
│                                                              │
│ 3. 사용자 질문 처리 시 관련 과거 대화 검색                   │
│    └── 유사도 기반 검색으로 관련 대화 추가 참조              │
└─────────────────────────────────────────────────────────────┘
```

##### 컨텍스트 주입 방식
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| P-09 | 자동 요약 생성 | 각 대화 종료 시 LLM이 핵심 내용 요약 생성 | P0 |
| P-10 | 프로젝트 컨텍스트 | 프로젝트 내 모든 대화 요약을 시스템 프롬프트에 포함 | P0 |
| P-11 | 관련 대화 검색 | 현재 질문과 유사한 과거 대화 내용 검색 | P1 |
| P-12 | 컨텍스트 토큰 관리 | 컨텍스트 크기 제한 및 우선순위 관리 | P0 |
| P-13 | 수동 참조 | 사용자가 특정 과거 대화를 명시적으로 참조 | P2 |

##### 컨텍스트 주입 형식
```
System Prompt에 추가되는 내용:

---
[Project Context: 논문 작성 프로젝트]

이 프로젝트의 이전 대화에서 확립된 맥락:

## 대화 1: 서론 작성 도움 (2026-01-15)
- 연구 주제: 조현병의 신경영상학적 바이오마커
- 핵심 가설: DMN 연결성 감소가 음성 증상과 연관
- 사용할 방법론: resting-state fMRI

## 대화 2: 방법론 검토 (2026-01-20)  
- 분석 도구: FSL, SPM12 사용 결정
- 전처리 파이프라인 확정
- 통계 검정: FDR correction 적용

## 대화 3: 결과 분석 (2026-01-25)
- 주요 발견: 전두-두정 연결성 20% 감소
- 해석상 주의점: 약물 효과 고려 필요
---

위 맥락을 참고하여 현재 대화에 응답하세요.
```

##### 토큰 관리 전략
```
총 컨텍스트 예산: Context Length의 30% 할당 (예: 64k → 19k tokens)

우선순위:
1. 가장 최근 대화 요약 (높음)
2. 현재 질문과 유사도 높은 과거 내용 (중간)
3. 오래된 대화 요약 (낮음)

토큰 초과 시:
- 낮은 우선순위부터 제거
- 요약을 더 압축
- 사용자에게 알림: "일부 과거 맥락이 생략되었습니다"
```

#### 3.4.5 프로젝트 설정
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| P-14 | 컨텍스트 참조 토글 | 프로젝트별 컨텍스트 참조 ON/OFF | P0 |
| P-15 | 컨텍스트 양 조절 | 참조할 과거 대화 수/토큰 제한 | P1 |
| P-16 | 커스텀 지시사항 | 프로젝트별 시스템 프롬프트 추가 | P1 |
| P-17 | 기본 모델 설정 | 프로젝트별 기본 사용 모델 지정 | P2 |

#### 3.4.6 프로젝트 UI

##### 사이드바 구조
```
┌─────────────────────┐
│  🐕 Ollama          │
│                     │
│  ┌───────────────┐  │
│  │  + New Chat   │  │
│  └───────────────┘  │
│                     │
│  ══ Projects ══════ │
│                     │
│  📁 논문 작성        │  ← 클릭 시 확장
│    │ 💬 서론 작성    │
│    │ 💬 방법론 검토  │
│    │ + 새 대화       │
│                     │
│  📁 웹 개발         │  ← 접힌 상태
│                     │
│  + 새 프로젝트       │
│                     │
│  ══════════════════ │
│                     │
│  ── This week ───── │
│  💬 일반 대화 1      │  ← 프로젝트 외 대화
│  💬 일반 대화 2      │
│                     │
└─────────────────────┘
```

##### 프로젝트 생성 모달
```
┌─────────────────────────────────────────┐
│  Create New Project                  ✕  │
├─────────────────────────────────────────┤
│                                         │
│  Name                                   │
│  ┌─────────────────────────────────┐   │
│  │ 논문 작성 프로젝트               │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Description (optional)                 │
│  ┌─────────────────────────────────┐   │
│  │ 조현병 신경영상 연구 논문 작성   │   │
│  │ 관련 모든 대화                   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Icon                                   │
│  📁 📚 🔬 💻 🎨 📝 🧠 ⚗️           │
│                                         │
│  Custom Instructions (optional)         │
│  ┌─────────────────────────────────┐   │
│  │ 학술적 어조로 응답하고,         │   │
│  │ APA 인용 형식을 사용해주세요.   │   │
│  └─────────────────────────────────┘   │
│                                         │
│  Context Settings                       │
│  ☑️ 과거 대화 자동 참조                 │
│  참조 대화 수: [5 ▾]                    │
│  최대 토큰: [10000 ▾]                   │
│                                         │
│         [Cancel]  [Create Project]      │
└─────────────────────────────────────────┘
```

##### 프로젝트 컨텍스트 표시
```
채팅 화면 상단:
┌─────────────────────────────────────────────────────────────┐
│  📁 논문 작성 프로젝트                                       │
│  ├── 참조 중: 3개 대화 (약 8,500 tokens)              [⚙️]  │
│  └── 💬 서론 작성 · 💬 방법론 검토 · 💬 결과 분석           │
└─────────────────────────────────────────────────────────────┘
│                                                              │
│  (채팅 내용...)                                              │
```

### 3.5 프롬프트 템플릿 (Slash Commands)

#### 3.5.1 개념
자주 사용하는 프롬프트를 `{key, prompt}` 형태로 저장하고, 입력창에서 `/`를 입력하면 저장된 템플릿 목록이 나타나 빠르게 선택할 수 있는 기능.

```
사용 흐름:
┌─────────────────────────────────────────────────────────────┐
│ 1. 입력창에 "/" 입력                                         │
│    ↓                                                        │
│ 2. 저장된 템플릿 목록 팝업 표시                               │
│    ↓                                                        │
│ 3. 키워드로 필터링 또는 방향키/클릭으로 선택                   │
│    ↓                                                        │
│ 4. 선택한 템플릿이 입력창에 삽입                              │
│    ↓                                                        │
│ 5. 변수 부분({variable}) 편집 후 전송                        │
└─────────────────────────────────────────────────────────────┘
```

#### 3.5.2 템플릿 구조
```typescript
interface PromptTemplate {
  id: string;
  key: string;              // slash command 키 (예: "summarize", "translate")
  title: string;            // 표시 이름 (예: "문서 요약")
  prompt: string;           // 실제 프롬프트 (변수 포함 가능)
  description?: string;     // 설명 (팝업에 표시)
  category?: string;        // 분류 (예: "학술", "개발", "번역")
  variables?: Variable[];   // 변수 정의
  projectId?: string;       // 특정 프로젝트 전용 (null이면 전역)
  createdAt: Date;
  updatedAt: Date;
}

interface Variable {
  name: string;             // 변수명 (예: "language")
  placeholder: string;      // 힌트 (예: "번역할 언어를 입력하세요")
  defaultValue?: string;    // 기본값
}
```

#### 3.5.3 템플릿 예시
```
┌─────────────────────────────────────────────────────────────┐
│ Key: summarize                                               │
│ Title: 문서 요약                                             │
│ Category: 학술                                               │
│ Prompt:                                                      │
│   다음 내용을 {length} 분량으로 요약해주세요.                 │
│   핵심 포인트를 불릿 포인트로 정리하고,                       │
│   주요 결론을 마지막에 한 문장으로 요약해주세요.              │
│                                                              │
│   내용:                                                      │
│   {content}                                                  │
│                                                              │
│ Variables:                                                   │
│   - length: "요약 분량 (예: 3문장, 500자)"                   │
│   - content: "요약할 내용을 붙여넣으세요"                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Key: translate                                               │
│ Title: 번역                                                  │
│ Category: 번역                                               │
│ Prompt:                                                      │
│   다음 텍스트를 {target_lang}로 번역해주세요.                │
│   원문의 뉘앙스와 문체를 최대한 유지해주세요.                 │
│                                                              │
│   {text}                                                     │
│                                                              │
│ Variables:                                                   │
│   - target_lang: "한국어" (기본값)                           │
│   - text: "번역할 텍스트"                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ Key: review                                                  │
│ Title: 코드 리뷰                                             │
│ Category: 개발                                               │
│ Prompt:                                                      │
│   다음 코드를 리뷰해주세요.                                  │
│   - 버그나 잠재적 문제점                                     │
│   - 성능 개선 포인트                                         │
│   - 코드 스타일 및 가독성                                    │
│   - 보안 취약점                                              │
│                                                              │
│   ```{language}                                              │
│   {code}                                                     │
│   ```                                                        │
└─────────────────────────────────────────────────────────────┘
```

#### 3.5.4 기능 요구사항

##### 템플릿 관리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| T-01 | 템플릿 생성 | 새 프롬프트 템플릿 추가 | P0 |
| T-02 | 템플릿 편집 | 기존 템플릿 수정 | P0 |
| T-03 | 템플릿 삭제 | 템플릿 제거 | P0 |
| T-04 | 템플릿 목록 | 저장된 템플릿 목록 보기 | P0 |
| T-05 | 카테고리 관리 | 템플릿 분류 카테고리 설정 | P1 |
| T-06 | 템플릿 가져오기/내보내기 | JSON 형식으로 백업/복원 | P2 |

##### Slash Command 인터페이스
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| T-07 | "/" 트리거 | 입력창에서 "/" 입력 시 팝업 표시 | P0 |
| T-08 | 실시간 필터링 | "/trans" 입력 시 "translate" 필터링 | P0 |
| T-09 | 키보드 네비게이션 | 방향키로 선택, Enter로 확정, Esc로 취소 | P0 |
| T-10 | 마우스 선택 | 클릭으로 템플릿 선택 | P0 |
| T-11 | 카테고리 그룹화 | 팝업에서 카테고리별 그룹 표시 | P1 |
| T-12 | 최근 사용 우선 | 자주/최근 사용한 템플릿 상단 표시 | P1 |

##### 변수 처리
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| T-13 | 변수 하이라이트 | 삽입된 템플릿의 {variable} 부분 강조 | P0 |
| T-14 | Tab 이동 | Tab 키로 다음 변수로 이동 | P0 |
| T-15 | 기본값 적용 | 변수에 기본값이 있으면 자동 삽입 | P1 |
| T-16 | 변수 힌트 표시 | 변수 위치에 placeholder 힌트 표시 | P1 |

##### 프로젝트 연동
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| T-17 | 프로젝트 전용 템플릿 | 특정 프로젝트에서만 표시되는 템플릿 | P1 |
| T-18 | 전역 템플릿 | 모든 대화에서 사용 가능한 템플릿 | P0 |

#### 3.5.5 UI 설계

##### Slash Command 팝업
```
입력창에 "/sum" 입력 시:

┌─────────────────────────────────────────────────────────────┐
│  /sum                                                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  📋 Recent                                          │    │
│  │  ────────────────────────────────────────────────── │    │
│  │  ▶ /summarize  문서 요약                            │    │ ← 선택됨
│  │    "내용을 요약하고 핵심 포인트 정리"               │    │
│  │                                                     │    │
│  │  📚 학술                                            │    │
│  │  ────────────────────────────────────────────────── │    │
│  │    /summarize  문서 요약                            │    │
│  │    /citation   인용 형식 변환                       │    │
│  │                                                     │    │
│  │  💻 개발                                            │    │
│  │  ────────────────────────────────────────────────── │    │
│  │    /review     코드 리뷰                            │    │
│  │    /debug      디버깅 도움                          │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ↑↓ 이동  Enter 선택  Esc 취소                              │
└─────────────────────────────────────────────────────────────┘
```

##### 템플릿 삽입 후 변수 편집
```
┌─────────────────────────────────────────────────────────────┐
│                                                              │
│  다음 내용을 [3문장        ] 분량으로 요약해주세요.          │
│  핵심 포인트를 불릿 포인트로 정리하고,          ↑            │
│  주요 결론을 마지막에 한 문장으로 요약해주세요.  Tab으로 이동 │
│                                                              │
│  내용:                                                       │
│  [요약할 내용을 붙여넣으세요                              ]  │
│                                              ↑ 현재 편집 중   │
│                                                              │
├─────────────────────────────────────────────────────────────┤
│  📎  🔍  │ qwen3:latest  ▾ │                           ➤    │
└─────────────────────────────────────────────────────────────┘
```

##### Settings > Prompt Templates 화면
```
┌─────────────────────────────────────────────────────────────┐
│  Settings                                                ✕  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ General  │ │ Models   │ │Templates │ │ About    │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
│                    ↑ 선택됨                                  │
│                                                              │
│  ─────────────────────────────────────────────────────────  │
│                                                              │
│  Prompt Templates                          [+ New Template]  │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 📚 학술 (3)                                    [▼]  │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  /summarize    문서 요약           [Edit] [Delete]  │    │
│  │  /citation     인용 형식 변환      [Edit] [Delete]  │    │
│  │  /abstract     초록 작성           [Edit] [Delete]  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 💻 개발 (2)                                    [▼]  │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  /review       코드 리뷰           [Edit] [Delete]  │    │
│  │  /debug        디버깅 도움         [Edit] [Delete]  │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  ─────────────────────────────────────────────────────────  │
│                                                              │
│  [Import Templates]  [Export Templates]                      │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

##### 템플릿 편집 모달
```
┌─────────────────────────────────────────────────────────────┐
│  Edit Template                                           ✕  │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  Command Key                                                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ / summarize                                         │    │
│  └─────────────────────────────────────────────────────┘    │
│  ⚠️ 영문 소문자, 숫자, 언더스코어만 사용                     │
│                                                              │
│  Title                                                       │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 문서 요약                                           │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  Category                                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 학술                                            ▾   │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  Description (optional)                                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 내용을 요약하고 핵심 포인트를 정리합니다             │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  Prompt                                                      │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ 다음 내용을 {length} 분량으로 요약해주세요.         │    │
│  │ 핵심 포인트를 불릿 포인트로 정리하고,               │    │
│  │ 주요 결론을 마지막에 한 문장으로 요약해주세요.      │    │
│  │                                                     │    │
│  │ 내용:                                               │    │
│  │ {content}                                           │    │
│  └─────────────────────────────────────────────────────┘    │
│  💡 {변수명} 형식으로 변수를 추가할 수 있습니다              │
│                                                              │
│  Variables                                    [+ Add Variable]│
│  ┌─────────────────────────────────────────────────────┐    │
│  │ {length}                                            │    │
│  │   Placeholder: 요약 분량 (예: 3문장, 500자)         │    │
│  │   Default: 3문장                              [✕]   │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │ {content}                                           │    │
│  │   Placeholder: 요약할 내용을 붙여넣으세요           │    │
│  │   Default: (없음)                             [✕]   │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                              │
│  Scope                                                       │
│  ◉ Global (모든 대화에서 사용)                               │
│  ○ Project only: [프로젝트 선택 ▾]                          │
│                                                              │
│              [Cancel]              [Save Template]           │
└─────────────────────────────────────────────────────────────┘
```

#### 3.5.6 기본 제공 템플릿
앱 설치 시 기본 제공되는 템플릿:

| Key | Title | Category | 설명 |
|-----|-------|----------|------|
| /summarize | 문서 요약 | 학술 | 내용 요약 및 핵심 포인트 정리 |
| /translate | 번역 | 번역 | 다국어 번역 |
| /explain | 쉽게 설명 | 일반 | 복잡한 개념을 쉽게 설명 |
| /review | 코드 리뷰 | 개발 | 코드 분석 및 개선점 제안 |
| /debug | 디버깅 | 개발 | 에러 분석 및 해결책 제시 |
| /refactor | 리팩토링 | 개발 | 코드 개선 및 최적화 |
| /grammar | 문법 교정 | 글쓰기 | 문법 오류 수정 |
| /rewrite | 다시 쓰기 | 글쓰기 | 문체/톤 변경하여 재작성 |

### 3.6 내보내기 기능

#### 3.6.1 복사 기능
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| E-01 | 메시지 복사 | 개별 메시지 복사 | P0 |
| E-02 | 코드 블록 복사 | 코드 블록 전용 복사 버튼 | P0 |

#### 3.6.2 내보내기 기능
| ID | 기능 | 설명 | 우선순위 |
|----|------|------|----------|
| E-03 | 마크다운 내보내기 | 전체 대화를 .md 파일로 저장 | P0 |
| E-04 | 파일명 자동 생성 | 날짜 + 대화 제목 기반 파일명 | P1 |
| E-05 | 프로젝트 전체 내보내기 | 프로젝트 내 모든 대화를 하나의 .md로 | P2 |

#### 3.6.3 마크다운 내보내기 형식
```markdown
# 대화 제목 (자동 생성 또는 첫 메시지 기반)

**날짜**: 2026-02-02 14:30:00  
**모델**: kimi-k2.5:cloud  

---

## User
[사용자 메시지 내용]

## Assistant
[AI 응답 내용]

---

## User
[다음 메시지...]

...
```

---

## 4. 기술 사양

### 4.1 기술 스택
| 분류 | 기술 | 버전 | 선택 이유 |
|------|------|------|-----------|
| Framework | Electron | ^28.0.0 | 크로스 플랫폼, 네이티브 기능 접근 |
| Frontend | React | ^18.2.0 | 컴포넌트 기반 UI, 생태계 |
| State | Zustand | ^4.5.0 | 간단하고 직관적인 상태 관리 |
| Styling | Tailwind CSS | ^3.4.0 | 유틸리티 기반, 빠른 스타일링 |
| Build | Vite | ^5.0.0 | 빠른 개발 서버, HMR |
| Bundler | electron-builder | ^24.0.0 | macOS 빌드 최적화 |
| Markdown | react-markdown | ^9.0.0 | 마크다운 렌더링 |
| Syntax | Prism.js | ^1.29.0 | 코드 하이라이팅 |
| DB | SQLite (better-sqlite3) | ^9.0.0 | 로컬 채팅 히스토리 저장 |

### 4.2 Ollama API 연동

#### 4.2.1 기본 채팅 API
```typescript
interface ChatRequest {
  model: string;
  messages: Message[];
  stream?: boolean;
  think?: boolean;
  tools?: Tool[];
}

interface Message {
  role: 'user' | 'assistant' | 'tool';
  content: string;
  thinking?: string;
  tool_calls?: ToolCall[];
  tool_name?: string;
  images?: string[]; // Base64 encoded
}
```

#### 4.2.2 Web Search Tool Schema
```typescript
const webSearchTool = {
  type: 'function',
  function: {
    name: 'webSearch',
    description: 'Performs a web search for the given query.',
    parameters: {
      type: 'object',
      properties: {
        query: { 
          type: 'string', 
          description: 'Search query string.' 
        },
        max_results: {
          type: 'number',
          description: 'Maximum number of results (default 3).',
        },
      },
      required: ['query'],
    },
  },
};

const webFetchTool = {
  type: 'function',
  function: {
    name: 'webFetch',
    description: 'Fetches content from a single URL.',
    parameters: {
      type: 'object',
      properties: {
        url: { 
          type: 'string', 
          description: 'URL to fetch.' 
        },
      },
      required: ['url'],
    },
  },
};
```

### 4.3 디렉토리 구조
```
ollama-desktop-pro/
├── package.json
├── electron/
│   ├── main.ts                 # Electron 메인 프로세스
│   ├── preload.ts              # Preload 스크립트
│   └── ipc/
│       ├── chat.ts             # 채팅 IPC 핸들러
│       ├── project.ts          # 프로젝트 IPC 핸들러
│       ├── template.ts         # 템플릿 IPC 핸들러
│       ├── files.ts            # 파일 처리 핸들러
│       └── settings.ts         # 설정 핸들러
├── src/
│   ├── main.tsx
│   ├── App.tsx
│   ├── components/
│   │   ├── Sidebar/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── ProjectList.tsx
│   │   │   ├── ProjectItem.tsx
│   │   │   ├── ChatHistory.tsx
│   │   │   └── NewChatButton.tsx
│   │   ├── Chat/
│   │   │   ├── ChatView.tsx
│   │   │   ├── ProjectContextBar.tsx  # 프로젝트 컨텍스트 표시
│   │   │   ├── MessageList.tsx
│   │   │   ├── Message.tsx
│   │   │   ├── MessageInput.tsx
│   │   │   ├── SlashCommandPopup.tsx  # "/" 명령어 팝업
│   │   │   ├── FileUpload.tsx
│   │   │   └── SearchToggle.tsx
│   │   ├── Project/
│   │   │   ├── ProjectModal.tsx       # 프로젝트 생성/편집
│   │   │   ├── ProjectSettings.tsx    # 프로젝트 설정
│   │   │   └── ContextSettings.tsx    # 컨텍스트 참조 설정
│   │   ├── Template/
│   │   │   ├── TemplateList.tsx       # 템플릿 목록 (Settings)
│   │   │   ├── TemplateModal.tsx      # 템플릿 편집 모달
│   │   │   └── VariableEditor.tsx     # 변수 편집 컴포넌트
│   │   ├── Settings/
│   │   │   └── SettingsModal.tsx
│   │   └── common/
│   │       ├── Markdown.tsx
│   │       ├── CodeBlock.tsx
│   │       └── LoadingDots.tsx
│   ├── hooks/
│   │   ├── useOllama.ts
│   │   ├── useChat.ts
│   │   ├── useProject.ts
│   │   ├── useProjectContext.ts       # 프로젝트 컨텍스트 관리
│   │   ├── useWebSearch.ts
│   │   ├── useTemplates.ts            # 템플릿 관리
│   │   └── useSlashCommand.ts         # Slash command 로직
│   ├── stores/
│   │   ├── chatStore.ts
│   │   ├── projectStore.ts
│   │   ├── templateStore.ts           # 템플릿 상태 관리
│   │   └── settingsStore.ts
│   ├── services/
│   │   ├── ollamaClient.ts
│   │   ├── webSearchAgent.ts
│   │   ├── fileProcessor.ts
│   │   ├── contextManager.ts          # 프로젝트 컨텍스트 생성/관리
│   │   ├── summarizer.ts              # 대화 요약 생성
│   │   └── templateEngine.ts          # 템플릿 변수 처리
│   ├── types/
│   │   └── index.ts
│   └── styles/
│       └── globals.css
├── resources/
│   ├── icon.icns
│   └── mascot.svg
└── database/
    └── schema.sql
```

### 4.4 데이터베이스 스키마
```sql
-- 프로젝트
CREATE TABLE projects (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    description TEXT,
    icon TEXT DEFAULT '📁',
    custom_instructions TEXT,        -- 프로젝트별 시스템 프롬프트
    default_model TEXT,              -- 기본 모델
    context_enabled BOOLEAN DEFAULT TRUE,
    context_max_chats INTEGER DEFAULT 5,
    context_max_tokens INTEGER DEFAULT 10000,
    archived BOOLEAN DEFAULT FALSE,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 채팅 세션
CREATE TABLE chats (
    id TEXT PRIMARY KEY,
    project_id TEXT,                 -- NULL이면 프로젝트 외 대화
    title TEXT,
    model TEXT NOT NULL,
    summary TEXT,                    -- 대화 요약 (컨텍스트 참조용)
    summary_updated_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE SET NULL
);

-- 메시지
CREATE TABLE messages (
    id TEXT PRIMARY KEY,
    chat_id TEXT NOT NULL,
    role TEXT NOT NULL,
    content TEXT NOT NULL,
    thinking TEXT,
    tool_calls TEXT, -- JSON
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (chat_id) REFERENCES chats(id) ON DELETE CASCADE
);

-- 첨부 파일
CREATE TABLE attachments (
    id TEXT PRIMARY KEY,
    message_id TEXT NOT NULL,
    filename TEXT NOT NULL,
    mime_type TEXT NOT NULL,
    content TEXT, -- Base64 or extracted text
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (message_id) REFERENCES messages(id) ON DELETE CASCADE
);

-- 대화 임베딩 (유사도 검색용, 선택적)
CREATE TABLE chat_embeddings (
    id TEXT PRIMARY KEY,
    chat_id TEXT NOT NULL,
    chunk_index INTEGER NOT NULL,
    content TEXT NOT NULL,
    embedding BLOB,                  -- 벡터 임베딩 (선택적)
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (chat_id) REFERENCES chats(id) ON DELETE CASCADE
);

-- 프롬프트 템플릿
CREATE TABLE prompt_templates (
    id TEXT PRIMARY KEY,
    key TEXT NOT NULL UNIQUE,           -- slash command (예: "summarize")
    title TEXT NOT NULL,
    prompt TEXT NOT NULL,
    description TEXT,
    category TEXT DEFAULT 'General',
    variables TEXT,                      -- JSON: [{name, placeholder, defaultValue}]
    project_id TEXT,                     -- NULL이면 전역 템플릿
    use_count INTEGER DEFAULT 0,         -- 사용 횟수 (정렬용)
    last_used_at DATETIME,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE
);

-- 설정
CREATE TABLE settings (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL
);

-- 인덱스
CREATE INDEX idx_chats_project_id ON chats(project_id);
CREATE INDEX idx_chats_updated_at ON chats(updated_at DESC);
CREATE INDEX idx_messages_chat_id ON messages(chat_id);
CREATE INDEX idx_templates_key ON prompt_templates(key);
CREATE INDEX idx_templates_project_id ON prompt_templates(project_id);
CREATE INDEX idx_templates_use_count ON prompt_templates(use_count DESC);
```

### 4.5 프로젝트 컨텍스트 관리 로직

#### 4.5.1 컨텍스트 생성 프로세스
```typescript
interface ProjectContext {
  projectId: string;
  projectName: string;
  customInstructions?: string;
  chatSummaries: ChatSummary[];
  totalTokens: number;
}

interface ChatSummary {
  chatId: string;
  title: string;
  date: string;
  summary: string;
  keyPoints: string[];
  tokenCount: number;
}

async function buildProjectContext(
  projectId: string,
  maxTokens: number,
  maxChats: number
): Promise<ProjectContext> {
  // 1. 프로젝트 정보 조회
  const project = await db.getProject(projectId);
  
  // 2. 최근 대화 조회 (업데이트 순)
  const chats = await db.getProjectChats(projectId, { 
    limit: maxChats,
    orderBy: 'updated_at DESC'
  });
  
  // 3. 각 대화의 요약 수집 (없으면 생성)
  const summaries: ChatSummary[] = [];
  let totalTokens = 0;
  
  for (const chat of chats) {
    if (!chat.summary || isStale(chat.summary_updated_at)) {
      chat.summary = await generateChatSummary(chat.id);
      await db.updateChatSummary(chat.id, chat.summary);
    }
    
    const tokenCount = estimateTokens(chat.summary);
    if (totalTokens + tokenCount > maxTokens) break;
    
    summaries.push({
      chatId: chat.id,
      title: chat.title,
      date: formatDate(chat.updated_at),
      summary: chat.summary,
      keyPoints: extractKeyPoints(chat.summary),
      tokenCount
    });
    totalTokens += tokenCount;
  }
  
  return {
    projectId,
    projectName: project.name,
    customInstructions: project.custom_instructions,
    chatSummaries: summaries,
    totalTokens
  };
}
```

#### 4.5.2 대화 요약 생성
```typescript
async function generateChatSummary(chatId: string): Promise<string> {
  const messages = await db.getChatMessages(chatId);
  
  const prompt = `
다음 대화를 분석하여 핵심 내용을 요약해주세요.

요약에 반드시 포함할 내용:
1. 대화의 주제와 목적
2. 주요 결정사항이나 결론
3. 중요한 정보나 데이터
4. 해결된 문제나 남은 과제

형식:
- 3-5개의 핵심 포인트로 요약
- 각 포인트는 1-2문장
- 전체 200단어 이내

대화 내용:
${formatMessagesForSummary(messages)}
`;

  const response = await ollama.chat({
    model: 'qwen3:latest',
    messages: [{ role: 'user', content: prompt }],
    stream: false
  });
  
  return response.message.content;
}
```

#### 4.5.3 시스템 프롬프트 생성
```typescript
function buildSystemPrompt(context: ProjectContext): string {
  let prompt = '';
  
  // 프로젝트 커스텀 지시사항
  if (context.customInstructions) {
    prompt += `## Project Instructions\n${context.customInstructions}\n\n`;
  }
  
  // 프로젝트 컨텍스트
  if (context.chatSummaries.length > 0) {
    prompt += `## Project Context: ${context.projectName}\n\n`;
    prompt += `이 프로젝트의 이전 대화에서 확립된 맥락:\n\n`;
    
    for (const summary of context.chatSummaries) {
      prompt += `### ${summary.title} (${summary.date})\n`;
      prompt += `${summary.summary}\n\n`;
    }
    
    prompt += `---\n위 맥락을 참고하여 현재 대화에 응답하세요.\n`;
  }
  
  return prompt;
}
```

#### 4.5.4 토큰 추정 함수
```typescript
function estimateTokens(text: string): number {
  // 대략적인 토큰 추정 (한글은 글자당 ~2토큰, 영어는 단어당 ~1.3토큰)
  const koreanChars = (text.match(/[가-힣]/g) || []).length;
  const englishWords = (text.match(/[a-zA-Z]+/g) || []).length;
  const numbers = (text.match(/\d+/g) || []).length;
  
  return Math.ceil(koreanChars * 2 + englishWords * 1.3 + numbers);
}
```

---

## 5. UI/UX 설계

### 5.1 디자인 원칙
1. **미니멀리즘**: 기존 Ollama 앱의 깔끔한 디자인 유지
2. **직관성**: 추가 기능이 기존 워크플로우를 방해하지 않음
3. **일관성**: macOS 디자인 가이드라인 준수
4. **접근성**: 키보드 네비게이션, 다크모드 지원

### 5.2 색상 팔레트
```css
:root {
  /* Light Mode */
  --bg-primary: #FFFFFF;
  --bg-secondary: #F5F5F5;
  --bg-tertiary: #EBEBEB;
  --text-primary: #1A1A1A;
  --text-secondary: #666666;
  --accent: #000000;
  --accent-hover: #333333;
  --border: #E0E0E0;
  --success: #22C55E;
  --warning: #F59E0B;
  --error: #EF4444;
  
  /* Dark Mode */
  --dark-bg-primary: #1A1A1A;
  --dark-bg-secondary: #252525;
  --dark-bg-tertiary: #333333;
  --dark-text-primary: #FFFFFF;
  --dark-text-secondary: #A0A0A0;
  --dark-accent: #FFFFFF;
  --dark-border: #404040;
}
```

### 5.3 와이어프레임

#### 5.3.1 메인 화면
```
┌──────────────────────────────────────────────────────────────┐
│  🔴 🟡 🟢        Ollama Desktop Pro              ⚙️          │
├────────────────┬─────────────────────────────────────────────┤
│                │                                             │
│  🐕 Ollama     │                                             │
│                │                                             │
│  ┌──────────┐  │              🐕                             │
│  │ + New    │  │                                             │
│  └──────────┘  │        Start a conversation                 │
│                │                                             │
│  ── This week ─│                                             │
│  💬 대화 제목 1│    ┌─────────────────────────────────────┐  │
│  💬 대화 제목 2│    │                                     │  │
│                │    │  Send a message...                  │  │
│  ── Older ─────│    │                                     │  │
│  💬 대화 제목 3│    │  📎  🔍  │ qwen3:latest  ▾ │  ➤  │  │
│                │    └─────────────────────────────────────┘  │
│                │                                             │
└────────────────┴─────────────────────────────────────────────┘
```

#### 5.3.2 채팅 화면 (웹 검색 활성화)
```
┌──────────────────────────────────────────────────────────────┐
│  🔴 🟡 🟢        Ollama Desktop Pro              ⚙️          │
├────────────────┬─────────────────────────────────────────────┤
│                │                                             │
│  🐕 Ollama     │  ┌─────────────────────────────────────┐   │
│                │  │ User                          📋    │   │
│  ┌──────────┐  │  │ 최근 AI 트렌드에 대해 알려줘        │   │
│  │ + New    │  │  └─────────────────────────────────────┘   │
│  └──────────┘  │                                             │
│                │  ┌─────────────────────────────────────┐   │
│  ── This week ─│  │ Assistant                    📋 📄  │   │
│  💬 AI 트렌드  │  │                                     │   │
│  💬 대화 제목 2│  │ 🔍 Searching: "AI trends 2026"      │   │
│                │  │ 📄 Fetching: techcrunch.com/...     │   │
│                │  │                                     │   │
│                │  │ 최근 AI 분야에서는...               │   │
│                │  │                                     │   │
│                │  │ **Sources:**                        │   │
│                │  │ [1] techcrunch.com/ai-2026          │   │
│                │  │ [2] arxiv.org/abs/2601.xxxxx        │   │
│                │  └─────────────────────────────────────┘   │
│                │                                             │
│                │    ┌─────────────────────────────────────┐  │
│                │    │  Send a message...                  │  │
│                │    │  📎  🔍✓ │ qwen3:latest ▾ │  ➤   │  │
│                │    └─────────────────────────────────────┘  │
└────────────────┴─────────────────────────────────────────────┘
```

#### 5.3.3 입력 영역 상세
```
┌─────────────────────────────────────────────────────────────────┐
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                                                           │  │
│  │  [첨부된 파일 미리보기]                                    │  │
│  │  ┌────────┐  ┌────────┐                                   │  │
│  │  │ 📄 doc │  │ 🖼️ img │  ✕                               │  │
│  │  └────────┘  └────────┘                                   │  │
│  │                                                           │  │
│  │  메시지를 입력하세요...                                    │  │
│  │                                                           │  │
│  ├───────────────────────────────────────────────────────────┤  │
│  │  📎 첨부   🔍 검색(ON)  │  kimi-k2.5:cloud  ▾  │    ➤    │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 5.4 컴포넌트 상세

#### 5.4.1 검색 토글 버튼
```
상태: OFF                    상태: ON
┌─────────────┐             ┌─────────────┐
│  🔍 Search  │             │  🔍 Search ✓│
│   (gray)    │             │  (accent)   │
└─────────────┘             └─────────────┘
```

#### 5.4.2 검색 진행 표시
```
┌─────────────────────────────────────────┐
│ 🔍 Generating search query...           │
│    ↓                                    │
│ 🔍 Searching: "AI trends 2026"          │
│    ↓                                    │
│ 📄 Fetching: techcrunch.com/...         │
│    ↓                                    │
│ ✅ Retrieved 3 sources                   │
└─────────────────────────────────────────┘
```

#### 5.4.3 메시지 복사 버튼
```
일반 상태:  📋 (투명)
호버 상태:  📋 (visible, tooltip: "Copy")
클릭 후:   ✅ (2초 후 원복)
```

---

## 6. 사용자 시나리오

### 6.1 기본 대화
1. 앱 실행
2. 모델 선택 (드롭다운)
3. 메시지 입력 후 전송
4. 스트리밍 응답 확인
5. 필요시 대화 계속

### 6.2 웹 검색 대화
1. 검색 토글 활성화 (🔍 버튼 클릭)
2. 질문 입력: "2026년 최신 AI 연구 동향은?"
3. LLM이 자동으로 검색어 생성
4. 검색 진행 상태 표시
5. 검색 결과 기반 응답 + 소스 링크 표시

### 6.3 파일 기반 대화
1. PDF 파일 드래그 앤 드롭
2. 파일 미리보기 확인
3. "이 문서를 요약해줘" 입력
4. 문서 내용 기반 응답

### 6.4 프로젝트 기반 대화 (신규)

#### 시나리오 A: 새 프로젝트 시작
1. 사이드바 하단 "+ 새 프로젝트" 클릭
2. 프로젝트 정보 입력:
   - 이름: "조현병 논문 작성"
   - 설명: "2026년 신경영상 연구 논문"
   - 아이콘: 🧠
   - 커스텀 지시사항: "학술적 어조, APA 형식 사용"
3. "Create Project" 클릭
4. 프로젝트 내에서 첫 대화 시작

#### 시나리오 B: 프로젝트 컨텍스트 활용
1. 기존 프로젝트 "조현병 논문 작성" 선택
2. "+ 새 대화" 클릭
3. 상단에 컨텍스트 표시: "참조 중: 3개 대화 (약 8,500 tokens)"
4. 질문: "앞서 논의한 방법론에서 전처리 파이프라인 다시 설명해줘"
5. LLM이 과거 대화의 방법론 검토 내용을 참조하여 응답
6. 추가 질문에도 일관된 맥락 유지

#### 시나리오 C: 컨텍스트 수동 조정
1. 프로젝트 대화 중 상단 컨텍스트 바의 ⚙️ 클릭
2. 참조할 대화 선택/해제 가능
3. 토큰 사용량 실시간 표시
4. 특정 대화만 참조하도록 조정

### 6.5 대화 내보내기
1. 대화 완료 후 📄 (내보내기) 버튼 클릭
2. 저장 위치 선택 다이얼로그
3. .md 파일로 저장
4. 파일명: `2026-02-02_AI트렌드대화.md`

### 6.6 프로젝트 전체 내보내기
1. 프로젝트 우클릭 → "Export All Chats"
2. 저장 위치 선택
3. 프로젝트 내 모든 대화가 하나의 .md 파일로 저장
4. 파일명: `조현병논문작성_프로젝트_전체대화.md`

### 6.7 Slash Command 사용

#### 시나리오 A: 템플릿으로 빠르게 질문
1. 입력창에 "/" 입력
2. 템플릿 목록 팝업 표시
3. "sum" 추가 입력 → "summarize" 필터링됨
4. Enter 또는 클릭으로 선택
5. 템플릿이 입력창에 삽입:
   ```
   다음 내용을 [3문장] 분량으로 요약해주세요.
   핵심 포인트를 불릿 포인트로 정리하고,
   주요 결론을 마지막에 한 문장으로 요약해주세요.
   
   내용:
   [요약할 내용을 붙여넣으세요]
   ```
6. `[3문장]` 부분이 선택된 상태 → 원하는 분량 입력
7. Tab 키로 다음 변수 `[요약할 내용...]`으로 이동
8. 내용 붙여넣기 후 전송

#### 시나리오 B: 새 템플릿 만들기
1. Settings → Templates 탭 이동
2. "+ New Template" 클릭
3. 템플릿 정보 입력:
   - Key: `paper_review`
   - Title: 논문 리뷰
   - Category: 학술
   - Prompt: `다음 논문의 {section} 부분을 비판적으로 검토해주세요...`
4. Variables에서 `{section}` 자동 감지
5. Placeholder 입력: "검토할 섹션 (예: 방법론, 결과)"
6. Save Template 클릭
7. 이후 `/paper_review`로 빠르게 사용

---

## 7. 에러 처리

### 7.1 에러 유형 및 메시지
| 상황 | 메시지 | 액션 |
|------|--------|------|
| Ollama 미실행 | "Ollama is not running. Please start Ollama first." | 재연결 버튼 |
| 모델 미설치 | "Model 'xxx' is not installed." | 모델 목록 새로고침 |
| 네트워크 오류 | "Unable to connect to Ollama server." | 설정 확인 안내 |
| 검색 실패 | "Web search failed. Responding without search results." | 일반 응답으로 폴백 |
| 파일 크기 초과 | "File is too large (max 50MB)." | 다시 선택 안내 |
| 지원 안하는 파일 | "Unsupported file type." | 지원 형식 안내 |

### 7.2 에러 UI
```
┌─────────────────────────────────────┐
│  ⚠️  Connection Error               │
│                                     │
│  Unable to connect to Ollama server │
│  at localhost:11434                 │
│                                     │
│  [Retry]  [Open Settings]           │
└─────────────────────────────────────┘
```

---

## 8. 성능 요구사항

| 항목 | 목표 | 측정 방법 |
|------|------|-----------|
| 앱 시작 시간 | < 2초 | Cold start |
| 첫 토큰 응답 | < 500ms | 메시지 전송 후 |
| 파일 업로드 | < 3초 (10MB) | 드롭~표시 |
| 메모리 사용 | < 500MB | 기본 사용 시 |
| CPU 유휴 | < 1% | 대화 없을 때 |

---

## 9. 보안 고려사항

1. **로컬 우선**: 모든 대화 데이터는 로컬 SQLite에만 저장
2. **네트워크 제한**: 웹 검색 시에만 외부 연결
3. **API 키 없음**: Ollama 로컬 API만 사용
4. **파일 접근 제한**: 사용자가 명시적으로 선택한 파일만 접근

---

## 10. 향후 계획 (Out of Scope)

| 버전 | 기능 |
|------|------|
| v1.1 | 다크 모드 |
| v1.2 | 템플릿 마켓플레이스 (커뮤니티 공유) |
| v1.3 | 대화 공유 (링크 생성) |
| v2.0 | 멀티 에이전트 (MCP 지원) |

---

## 11. 성공 지표

| 지표 | 목표 |
|------|------|
| 일일 사용 시간 | 개인 측정 |
| 검색 기능 사용률 | > 30% |
| 내보내기 사용률 | > 20% |
| 크래시 비율 | < 0.1% |

---

## 12. 부록

### 12.1 참고 자료
- [Ollama API Documentation](https://github.com/ollama/ollama/blob/main/docs/api.md)
- [Ollama JavaScript Library](https://github.com/ollama/ollama-js)
- [Electron Documentation](https://www.electronjs.org/docs)

### 12.2 용어 정의
| 용어 | 정의 |
|------|------|
| Ollama | 로컬 LLM 실행 플랫폼 |
| Tool Calling | LLM이 외부 도구를 호출하는 기능 |
| Streaming | 실시간 토큰 단위 응답 전송 |
| Context Length | 모델이 기억하는 토큰 수 |

---

**문서 끝**
